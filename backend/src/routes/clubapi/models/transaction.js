// Single source of truth for the ClubAPITransaction schema. This path is kept
// so existing imports keep working; the definition lives in src/models.
export { default } from '../../../models/ClubAPITransaction.js';
